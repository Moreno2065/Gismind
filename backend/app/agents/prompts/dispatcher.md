# Dispatcher role prompt (Phase 4: full context-aware task planning)

Currently uses inline DISPATCHER_PROMPT in `app/agents/dispatcher.py`.
This file is a placeholder for the Phase 4 external prompt loaded via registry.
